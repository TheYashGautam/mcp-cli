{
  "name": "mcp-cli",
  "version": "0.1.0",
  "description": "Homebrew for MCP — install, configure, and manage Model Context Protocol servers with one command.",
  "type": "module",
  "bin": {
    "mcp": "./bin/mcp.js"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "scripts": {
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "commander": "^12.1.0"
  }
}
